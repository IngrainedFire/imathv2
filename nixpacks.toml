[phases.setup]
packages = ["nodejs_22", "npm"]

[phases.install]
commands = [
  "npm install"
]

[phases.build]
commands = [
  "npm run build"
]

[phases.start]
cmd = "node .output/server/index.mjs"

[env]
PORT = "3000"
NODE_ENV = "production"
